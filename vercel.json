{
  "buildCommand": "cd EcommerceApp/client && npm install && npm run build",
  "outputDirectory": "EcommerceApp/client/dist",
  "installCommand": "cd EcommerceApp/client && npm install",
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
